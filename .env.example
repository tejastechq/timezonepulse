# --- Environment Variable Configuration ---
# This file serves as an example. DO NOT commit real secrets here.
# For Vercel deployments, manage secrets securely using Vercel's Environment Variables settings:
# https://vercel.com/docs/projects/environment-variables
# Use strong, unique values for each environment (Production, Preview, Development).

# Application
NODE_ENV=development # Set to 'production' in Vercel Production/Preview environments
NEXT_PUBLIC_APP_URL=http://localhost:3000 # Update with your production URL in Vercel

# Security Configuration
# IMPORTANT: Use Vercel's Environment Variables UI for these secrets.
# Generate strong, random secrets (e.g., using `openssl rand -base64 32`).
SESSION_SECRET=your_secure_session_secret_min_32_chars_long # Example: Replace with a real secret in Vercel
ADMIN_API_SECRET=your_secure_admin_secret_min_32_chars_long # Example: Replace with a real secret in Vercel
# CSP_REPORT_URL is handled by the API route /api/csp-report, no need to set externally unless using a third-party service.
# CSP_REPORT_URL=https://your-csp-reporting-endpoint.com/report

# Rate Limiting
RATE_LIMIT_WINDOW=60
RATE_LIMIT_MAX_REQUESTS=60

# CORS Configuration (comma-separated values)
CORS_ALLOWED_ORIGINS=http://localhost:3000
CORS_ALLOWED_METHODS=GET,POST,OPTIONS

# Cookie Security
COOKIE_SECURE=true
COOKIE_SAME_SITE=strict
