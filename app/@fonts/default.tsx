/**
 * Default component for the @fonts parallel route
 * This is loaded by Next.js when no other page is specified in this slot
 */
export default function FontsDefault() {
  // This component doesn't render anything visible
  // It just provides a default implementation for the fonts slot
  return null;
} 